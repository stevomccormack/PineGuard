// turbo-all

> **Note:** `tools/testing/auto/Run-Last.ps1` is planned but not yet implemented.
> When available, this workflow will re-execute the last test command.

1. Run Latest Test Command
   pwsh -File tools/testing/auto/Run-Last.ps1
