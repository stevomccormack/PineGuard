# Knowledge and tooling closeout ledger

## CODEEX-01 — Code-example amendment, 2026-09-26

**User correction and outcome:** the plan lacked actual code examples. The amendment supplies a complete canonical reference and substantive direct examples in all 23 workstreams and three app subplans. Current APIs are source-inspected insertion units; the separately compiled miniature is explicitly a proposed teaching design. Product implementation, roadmap decisions and readiness claims remain unchanged.

**Saved lessons:** helper-only calls such as invoking a hidden check are not sufficiently actionable examples; include the relevant implementation/assertion/configuration with declared context and independent outcomes. Caveats do not excuse avoidable defects in an advertised invariant: use a real ordinary-construction boundary in the illustration. Separate instrumentation cost and evidence scope from performance claims. Gate examples must reject empty/stale/mislabeled evidence, not merely check that a file exists.

**Model/route observation:** an external Sol-runner attempt was rejected before process start for unestablished disclosure handling. It was not retried or bypassed. A subsequently available, approved native Sol subagent produced the code under the user's role policy. Prefer the available approved native role channel; changing capacity or a past successful route is not authorization to repeat a rejected action. This is a session observation, not a new global rule hierarchy.

**Roles and exact scope:** Sol authored Pilot.csproj, Program.cs and governance script examples; Luna read source/BRAIN and verified frozen artifacts; Astra reviewed design, code deltas and documentation classification. All runnable verification artifacts and bin/obj/fixtures stayed in the workspace. Published changes are documentation and fenced source examples only; no repository product/tool implementation is claimed.

**Verified outcomes:** SDK 10.0.401; isolated net10.0 Release build exited 0 with zero warnings/errors; run exited 0 with the complete demonstration-check PASS line and consumed=100000. Timing/allocation numbers are observations, not performance acceptance. The PowerShell governance example self-test exited 0 with success cases and ten expected rejection probes: wrong identity, wrong revision, below-100% coverage, invalid count, duplicate expected identity, missing artifact, whitespace-only API, API drift, missing API and missing lock before restore/network. Final source hashes and reproducible source are recorded in [Code examples](code-examples.md).

**Feedback closure:** accepted and corrected: helper-only excerpts; bypassable teaching value-object construction; measured instrumentation allocation; whitespace-only API input; evidence identity/count handling. Astra accepts the corrected design and scoped verification. Current PineGuard insertion snippets, real repository coverage, BenchmarkDotNet and NativeAOT remain unexecuted by this amendment and must not be reported as verified.

**Publication checks pending publisher:** exact documentation manifest; master v1.9/child v1.2 Planned; 26 substantive direct examples and links; complete portable source fences; final frozen hash equality; current/proposed verification labels; links/fences/whitespace; protected-file/staging ownership and final commit. Record actual publication results before claiming this block fully closed. Existing PROMOTE-01 governs any later reusable-rule promotion; no unrelated agent-memory file is created.

Project-specific closeout records live here because no applicable planner/execution memory home was established by the bounded BRAIN inspection. This is not a competing repository memory system. Promote reusable observations through the canonical taxonomy/spec process in [closure contract](knowledge-and-tooling-closure.md).

## Planning session record — 2026-09-26

**Unit:** readiness modular planning and documentation publication. **Prior publication commit:** `f3adcb34` (reported by publisher). **Current amendment:** planning-only knowledge/tooling closure requirement; final amended publication checks/commit are pending publisher execution.

**Constraint and rationale:** the user requires BRAIN, all relevant docs, scripts and tools to be maintained at every workstream/major-block/wave boundary, with saved lessons and a full review→feedback→fix→recheck loop. This is essential to prevent apparently complete features leaving stale execution knowledge or unusable support machinery. Binding project destinations are the quality constitution and closure contract; general repo-wide promotion follows existing specification cascade rather than duplicated new rules.

### Saved observations and outcomes

| Observation | Evidence / outcome | Scope and durable handling |
|---|---|---|
| Native delegation hit an agent-thread limit even after an intake agent completed | Root and Astra each received the explicit limit failure; repeated retries were stopped | Session/tool behavior, not a universal concurrency theorem. Future orchestrators should record actual available capacity and checkpoint rather than assume a completed agent frees a slot |
| Required Sol comparison was produced by a genuine local Sol runner | Luna verified runner options/model and supplied exact output; model sandbox remained read-only; runtime initialization required separately authorized host access | A verified fallback in this session, not permission to bypass sandbox/model policy. Preserve role/model evidence; reverify availability before reuse |
| Immutable historical sources contain whitespace that differs from authored-document hygiene requirements | Exact snapshots/Sol source retained; authored checks and frozen-source exceptions were reported separately with hash evidence | Preserve source integrity and honest check scope. Never claim full clean whitespace output when exceptions exist; follow applicable governing policy |
| Dirty-tree ownership requires explicit file/hunk scope | Protected original files/hashes remained unchanged; only owned plan files and canonical index hunk were staged | Apply existing safety/commit rules; this observation does not invent a parallel staging policy |
| User priorities changed during drafting | Greenfield, early taxonomy/structure/manifest/benchmarks, hard validation coverage and late major samples replaced stale assumptions | Updated master/decisions/children; frozen source inputs remain historical with explicit supersession |
| Pilot language initially risked implying final pilot-only completion | Astra self-review added all-declared-family contract/manifest rollout and final full-scope conformance gate | Review master/child completion consistency; record fixes rather than silently adjust expected scope |

No new global Spec/Rule or unrelated agent memory file is created by this amendment. The lessons above are saved observations. PROMOTE-01 is assigned to Astra at Wave A's first closure review: evaluate `docs/ai/specs/orchestration.md` as the execution/closure destination, or root `docs/ai/specs/spec.md` only under §9's cross-cutting test; record promote/no-promote rationale and perform affected child/Rule/adapter/index cascade if approved. The binding project requirement is already saved in the quality/closure contracts.

### Present impact and check status

Affected planning artifacts: master, all23 workstreams, all3 major-app subplans, quality/execution/decision/acceptance/estimates/integrated-review references, this ledger and closure contract. Exact amended manifest is supplied to the publisher. Relevant source specifications are cited without changing their content.

Prior publication: mechanical links/metadata/coverage-order/greenfield/effort arithmetic review passed; authored whitespace hygiene and immutable-source byte preservation were checked under the reported exception policy. No implementation, new tests, coverage or future supporting scripts were performed.

Current amendment checks: **PENDING publisher** — all 26 inherited closure links; explicit per-block and A–F wave completion language; master v1.8/child v1.1 metadata; exact 36-file amended manifest; separate closeout reserve arithmetic; authored whitespace; immutable/protected hashes; staged ownership and final commit. Save actual outcomes below before claiming closure; do not convert pending checks to complete from authoring alone. The final commit identity can be resolved from repository history and reported by the publisher after committing this accepted record, avoiding a self-referential hash inside its own commit.

### Feedback log

| ID | Review | Finding / disposition | Evidence / final state |
|---|---|---|---|
| PLAN-01 | Astra self-review | Pilot-only wording could understate final scope; accepted and fixed | W01/W02 rollout tasks and W05 full-scope gate; verified in prior mechanical QA |
| PLAN-02 | Luna mechanical QA / Astra | Joined prose spacing; accepted and corrected | Six exact master/acceptance fixes before prior publication |
| PLAN-03 | User correction / Astra | Knowledge/docs/tooling closure must be mandatory at each major unit; accepted | Current shared contract plus26 explicit gates and master wave gate; final publication validation pending |
| PLAN-04 | Astra scope review | Duplicating a repo-wide rule hierarchy or inventing an unrelated agent memory home; rejected | Existing taxonomy/spec-cascade references govern; session facts saved in this ledger |

### Publisher completion entry

- Amended exact paths and metadata versions: 36 changed paths, comprising the v1.8 master, nine shared references, W00–W22, and three W20 sample subplans; master v1.8 `planned`, all 26 child plans v1.1 `planned`.
- Link/metadata/closure inheritance and estimate checks: 26/26 child closure links present; six A–F wave rows present; 189 local Markdown links checked with zero broken links within the modular plan; zero unbalanced code-fence files. Master and child status/version metadata passed. Incremental reserve totals agree: core/light 204–430 engineering-equivalent, 70–166 agent/tool, 39–81 review hours; with major apps 267–556, 90–214, 48–98; A–F engineering wave ranges sum to 267–556.
- Authored whitespace result; explicit immutable-source exceptions: `git diff --cached --check` passed with exit code 0 for exactly 36 staged authored files. Historical source snapshots and research references are outside this amendment. Staged Git blob IDs matched raw worktree hashes for all 36 paths (zero mismatches).
- Snapshot/protected source and staged-blob hashes: prepublication HEAD was `f3adcb34b1ef52156f7c27e5ae5e885c649f9ebb`, with an empty index. Protected dirty/untracked file SHA-256 values matched the prior recorded baseline for `.codex/config.toml`, `README.md`, `docs/ai/plans/schema-driven-validation-agent.md`, `docs/ai/plans/technical-readiness-to-1.0.md`, `docs/reports/readme-verification-2026-09-25.md`, and `docs/reports/technical-readiness-rubric-2026-09-25.md`. Final staged plan file hashes and status are verified by the publisher before commit.
- Staged-diff ownership review: `git diff --cached --name-only` contained exactly the 36 amended plan paths and no other paths. The canonical index row already pointed to the new master and was left unchanged; pre-existing protected changes remain unstaged/untracked.
- Astra final acceptance/reopened findings: Astra accepted the amendment content after the mechanical spacing fixes; no reopened findings were reported.
- Amended commit ID and remaining open issues: record the commit ID in the completion report after commit; no implementation, source-code tests, build, or new coverage collection was performed for this documentation-only amendment. Any unexpected mismatch or check failure blocks commit and is reported rather than waived.
