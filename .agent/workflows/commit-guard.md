---
description: Run Scoped Git Commits (GuardClauses)
---

// turbo-all

1. Read and execute "docs/ai/agents/commit-guard.md".
2. `-Push` / `-SafePush` are Tier 1 and never turbo-eligible (`docs/ai/specs/safety.md` §8.4) — dry-run first (`-DryRun`) and ask the user before pushing.
