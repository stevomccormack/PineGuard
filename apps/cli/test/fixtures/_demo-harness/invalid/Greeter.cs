namespace Demo;

public static class Greeter
{
    // BADWORD: placeholder the demo rule must flag — see test/support/runRule.test.ts.
    public static string Hello(string name) => $"Hello, {name}!";
}
