# Valid fixture

See the [companion guide](./guide.md) for background, and read `docs/setup.md`
before making changes to this fixture.

Planned work not yet implemented: `+ some/planned/path.ts` (must not be
flagged — the `+ path` convention marks it as intentionally not-yet-existing).

Also see docs/setup.md directly in prose, with no backticks or link syntax,
to prove the bare-prose extraction path also resolves a real target.
