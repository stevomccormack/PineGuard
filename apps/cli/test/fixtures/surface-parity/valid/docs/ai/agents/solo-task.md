# Solo Task

A fake agent playbook belonging to no declared Copilot family, required only on the
FakeClaude surface per the fixture's "Solo family" exception — used only by the
`surface-parity` rule's `valid/` fixture.
