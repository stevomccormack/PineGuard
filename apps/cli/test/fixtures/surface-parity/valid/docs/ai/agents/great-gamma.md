# Great Gamma

A fake agent playbook belonging to the "gamma" family (as a _suffix_, not a prefix — mirrors
the real repo's `ask-council`/`plan-with-council` shape), used only by the `surface-parity`
rule's `valid/` fixture.
