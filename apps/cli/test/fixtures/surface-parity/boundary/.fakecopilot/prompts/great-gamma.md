One of two "gamma" family representatives present on the FakeCopilot surface (boundary:
too many — exactly one is expected). There is deliberately no "alpha" representative at all
here (boundary: too few — also exactly one is expected).
