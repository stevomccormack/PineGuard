# Solo Task

A fake agent playbook. In the `valid/` fixture this is exempted to the FakeClaude surface
only by a declared exception; here the exceptions section is missing entirely, so no
exemption applies.
